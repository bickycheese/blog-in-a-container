{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "http://localhost:4000",
            "preLaunchTask": "Start jekyll",
            "postDebugTask": {
                "type": "shell",
                "command": "kill $(pidof -s ruby2.7)" // not working!
            }
        }
    ]
}